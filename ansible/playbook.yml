- hosts: vps
  become: true

  tasks:
    - name: Install Docker
      apt:
        name: docker.io
        update_cache: yes

    - name: Install Docker Compose
      apt:
        name: docker-compose
        state: present

    - name: Enable Docker
      systemd:
        name: docker
        enabled: yes
        state: started